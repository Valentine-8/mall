<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.mapper.MallCategoryMapper">

    <resultMap type="MallCategory" id="MallCategoryResult">
        <result property="categoryId" column="category_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectCategoryVo">
        select category_id, parent_id, category_name, order_num, status, del_flag,
               create_by, create_time, update_by, update_time, remark
        from mall_category
    </sql>

    <select id="selectMallCategoryById" parameterType="Long" resultMap="MallCategoryResult">
        <include refid="selectCategoryVo"/>
        where category_id = #{categoryId} and del_flag = '0'
    </select>

    <select id="selectMallCategoryList" parameterType="MallCategory" resultMap="MallCategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            del_flag = '0'
            <if test="categoryName != null and categoryName != ''">
                AND category_name like concat('%', #{categoryName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
        </where>
        order by parent_id, order_num
    </select>

    <insert id="insertMallCategory" parameterType="MallCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into mall_category (
            parent_id, category_name, order_num, status, del_flag,
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            create_time
        ) values (
            #{parentId}, #{categoryName}, #{orderNum}, #{status}, '0',
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateMallCategory" parameterType="MallCategory">
        update mall_category
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>

    <update id="deleteMallCategoryById" parameterType="Long">
        update mall_category set del_flag = '2' where category_id = #{categoryId}
    </update>

    <update id="deleteMallCategoryByIds" parameterType="Long">
        update mall_category set del_flag = '2' where category_id in
        <foreach collection="array" item="categoryId" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </update>
</mapper>
